let x = -5;

let absoluteValue = Math.abs(x); // Returns the absolute value of x
let sign = Math.sign(x); // Returns the sign of x (1 if positive, -1 if negative, 0 if zero)

console.log(absoluteValue, sign);
